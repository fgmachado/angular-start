/*
    Global variables
*/

var mainModule = 'angularStart';

var module = {
    main: mainModule,
    core: mainModule + '.core',
    components: mainModule + '.components',
    controllers: mainModule + '.controllers',
    services: mainModule + '.services'
};